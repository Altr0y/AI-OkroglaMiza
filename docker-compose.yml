services:
  backend:
    build:
      context: ./backend
    develop:
      watch:
        - path: ./backend
          action: rebuild
    ports:
      - "5000:5000"
    env_file:
      - .env
    environment:
      - PORT=5000
    restart: unless-stopped
